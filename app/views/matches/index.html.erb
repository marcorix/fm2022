<h1>Matches</h1>
<ul>
  <% @matches.each do |match| %>
     <% text = " #{match.home_player.teams.last.name} VS #{match.away_player.teams.last.name}" %>
    <li>
      <%= link_to text,  match_path(match) %>
    </li>
  <% end %>
</ul>
